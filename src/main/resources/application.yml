server:
  port: ${SERVER_PORT:8090}
spring:
  application:
    name: bookstore
#  security:
#    oauth2:
#      client:
#        registration:
#          github:
#            clientId: 5f57c2ee5bd1b5b8668f
#            clientSecret: c6859c0a11b068b3471f219af3757815fba81e30
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://${RDS_HOST_NAME:192.168.0.248}:${RDS_PORT:3306}/bookstore?autoreconnect=true"
    username: ${RDS_USER_NAME:bookstore-user}
    password: ${RDS_PASSWORD:sawp~Fd385}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          timezone: UTC+2
        dialect: org.hibernate.dialect.MySQLDialect
    generate-ddl: false
  flyway:
    baseline-on-migrate: true
    enabled: true
    locations: classpath:db/migration
logging:
  level:
    org.springframework.security: TRACE


